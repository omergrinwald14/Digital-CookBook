# Repo-wide ignores (backend/.gitignore covers the Python venv + secrets too).

# Private session notes — local working file, not part of the project.
handoff.txt
PLAN.md
# Secrets, anywhere in the repo.
.env

# Python leftovers.
.venv/
__pycache__/
*.pyc

# OS junk.
Thumbs.db
.DS_Store

# Local Claude Code / editor config.
.claude/

# One-off thumbnail backfill (2026-09-17) and the originals it saved before
# overwriting. Both are local safety nets, not part of the project.
backend/backfill_thumbs.py
backend/backup/
